{
  "name": "teleprompter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "export": "nuxt export",
    "serve": "nuxt serve",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "caniuse-api": "^3.0.0",
    "nuxt": "^2.13.0",
    "socket.io": "^2.3.0",
    "socket.io-client": "^2.3.0"
  }
}
